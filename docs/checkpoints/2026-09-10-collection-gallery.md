# 도서·작품, 현장 스케치 and operating status checkpoint

Local review increment following `6c1c937e9565c199e23c61a4817d09f3c492ffd0`.

- Bilingual `/ko/collection`, `/en/collection` and numeric detail routes introduce the center's 도서·작품. Existing exhibition plates and footer link to the gallery. Public labels use 도서, and activity cards use 자세히 보기 / View details.
- `/ko/admin/collection` registers and edits books/artworks, creator, optional English content, Markdown descriptions, up to twelve ordered photos, display order and visibility. Shared uploads, form controls, confirmation and session reauthentication preserve the existing interaction/security model.
- Additive `collection_items` SQLite table retains existing events/highlights/notices. Drafts default private, publication requires a cover, image paths and real files are checked, and public routes/sitemap only return active rows. Direct image access also checks current public references; private-only images require an authenticated session. Uploads bypass the image optimizer and use no-store responses so unpublishing revokes subsequent anonymous requests. Shared active references retain access. The backup CLI now includes collection and inline Markdown images, including private records.
- Home program details now align with the tab row and divider. Visit uses a square left map with larger address/hours/contact headings stacked on the right. Page-number entry is a centered 64px field. The footer shop/meetup group is labeled 참여하기 / Get involved. Existing header/footer/card tokens and reduced-motion behavior are retained.
- Public activity content is named 현장 스케치 / Highlights, including headings, navigation, pagination, empty messages and return links. `/journal` URLs remain stable. Home and navigation place it before exhibition/experience. The wallet guide action uses the same text and right-arrow treatment as the adjacent collection link.
- The program detail's external participation link reads 참여하기 / Join, retaining its validated destination, external arrow and accessible new-window label.
- Header status appears before the language control and automatically shows 운영 중, 밋업 중 or 운영 종료 using Asia/Seoul time. Daily 12:00–20:00 hours include Sunday; published Korean public/substitute/temporary holidays close by default. Today's admin exception can enable ordinary operation or force temporary closure. Meetup windows extend outside ordinary hours and across midnight. Initial HTML and boundary/minute/focus refreshes use the server result; visitor clocks cannot override it. The indicator links to visit information and retains the brief, reduced-motion-safe feedback.
- Client Zod starts with `jitless: true` before hydration so public status validation does not probe dynamic code execution under the strict CSP. No unsafe-eval exception, new UI dependency, credential format change, production write or legacy runtime change. The automatic-status follow-up adds only the small server-side Korean holiday data package.

Validation and visual artifacts: `web/.local/collection-20260910/` (ignored). The final source passes build, typecheck/lint, 39 API/browser regression scenarios and 14 navigation/scroll scenarios. The backup CLI passes 11 checks, including a private collection cover and inline Markdown image. Dependency audit reports no vulnerabilities. Final rendered evidence is recorded separately; independent visual/system verdicts bind to the local commit in that directory's `ledger.md`.

Automatic-status follow-up: typecheck/lint/build pass; `npm run test:status` checks 34 schedule cases plus legacy time parsing and exact boundaries, and 28 live API/browser/security scenarios pass. After a mobile English fallback-label refinement, both live status scenarios pass again on the final build. Fresh header coverage enumerates all 14 public hosts, both locales/themes, responsive sizes, status states and motion; today's admin exceptions, time-entry guidance and shared login copy are also captured. Final evidence and exact-commit review results are in `automatic/evidence.json` and `ledger.md`.

Only temporary, clearly labeled QA collection and notice records are used for populated captures and removed afterward. Actual center holdings must be registered by the administrator. Status checks restore the original local selection. Preview remains noindex, including all admin routes. No push or deployment.
