# Publishing and public layout checkpoint — 2026-09-10

Local review build only, based on `ce066b9af6df7c5975e6f370f008ed1a10fe3c10`. SQLite and the legacy application remain in place. No production data, deployment, push or GitHub Actions operations.

## Result

- Events, highlights and notices support safe server-rendered Markdown and up to ten normalized hashtags. Additive SQLite columns preserve old rows. Plain-text excerpts feed cards and metadata.
- The shared editor attaches multiple photos through its toolbar or file drop at the writing position. It preserves concurrent typing, prevents saving while uploading and handles retry/session expiry. Inline images stay in the body rather than duplicating the cover gallery.
- Article titles, images and text share a centered reading column; the back link follows the outer left edge. Gallery and inline photos preserve their aspect ratios.
- Program events are grouped by date in muted surface cards with restrained shadows. The shared calendar supports direct year entry and twelve month buttons, including supported-range and nested Escape handling. Admin date entry uses the same calendar in a styled dialog. Checkboxes, radio controls, upload buttons and discard/delete dialogs share site styles and keyboard semantics.
- The header wordmark is larger; repeated visit CTA buttons are removed. Visit uses a wider address/map column with hours and contact stacked to its right; tablet and mobile layouts stack the map above the information. Korean copy uses 서재. Journal pagination allows centered direct page-number entry without scrolling the outgoing page. Journal card titles and reading links retain neutral colors on hover. Native smooth scrolling is scoped to deliberate hash targets, with explicit reduced-motion-aware top/calendar actions.
- Browser language selection follows explicit URL, remembered choice, Accept-Language and Korean fallback. Admin copy remains Korean.
- Footer has four desktop columns including the center's Saturday Block merchandise and meetup links, two columns on tablet and a stacked mobile layout. All four groups span the bottom divider width.
- Admin search exclusion is applied at the shared layout and HTTP response header; admin paths remain outside the sitemap. Existing search results require the later deployment/removal procedure in the SEO report.

## Verification evidence

Build `YhbqZ8l7O9Isdnb5bayHk` on loopback port 3102. Typecheck, lint and production build passed. Markdown checks: 41; authentication checks: 8; tag API/SQLite checks: 11; tag rendering checks: 7. Dependency audit reported no vulnerabilities. All 91 integrated browser checks passed before the final CSS-only visit regrouping (build `HBzHVIgLyVoPSAsBBTAR2`): `web/.local/ui-refinements-20260910/browser-tests-complete.log`. The final regrouping passed fresh type/lint/build checks (`check-visit-layout.log`, `build-visit-layout.log`) and the ko/en responsive map regression (`visit-layout-test.log`). The new map overflow regression was observed failing before the minimum grid width correction and now passes.

The fresh capture set contains 144 route/language/theme/viewport combinations, additional 320px/1120px/1760px edge widths, real notice publishing and reopening with an inline center photo, custom controls, and motion/reduced-motion sequences. `evidence.json` records the final PNG count, dimensions, hashes, contact sheets and 144 header baseline diffs. The set also includes month/year picking, neutral journal and meetup card hover/focus, and twelve real Google map states with both page languages against the opposite browser language. A temporary published notice was deleted after inspection.

Capture-only corrections: resting cards wait for their existing entrance animations; calendar captures derive the actual available year range; narrow Google embeds expose an open-map link instead of the wider directions link; hidden tab-background images are excluded from visible-image decoding; Korean radio labels are matched in Korean; authentication captures run serially because simultaneous password verification is rate limited. Full component crops omit fixed reading-progress/back-to-top overlays; normal viewport captures retain them. Chrome updater descendants also retained exited-browser stdout/stderr and delayed capture-process cleanup; the ignored harness now uses a loopback browser server and releases those streams only after browser exit. No product rendering was altered to hide a defect.

Independent final review verdicts bind to the committed SHA in the local evidence ledger; older review results do not cover this increment. The separate [SEO report](../handoff/2026-09-10-seo-status.md) records remaining release work and measurement limits.

The owner subsequently requested the Google map itself, replacing the address group's button. The verified keyless Google share iframe is now immediately under the address note; hours/contact stack beside the wider map in a 3:2 desktop layout, reducing the empty area. Map height stays between 288px and 384px. Korean and English map controls follow the page locale. The external footer map link remains. The parent CSP permits only `https://www.google.com/maps/embed` for frames, including client navigation from other pages; other script/image restrictions remain. Real map rendering, the center CID, embedded map controls and narrow/wide sizing were verified. No account, API key or paid Maps API was added.

The reported broken logo was a local review lifecycle bug: rebuilding overwrote the standalone directory used by the live server. `review start` now serves from its own temporary standalone copy, removed when that process exits. Original logo requests failed before the fix and remained successful through subsequent rebuilds. This changes local review operation only.

The last request about applying meetup-card framing to home journal cards was a design assessment: retain the large-photo vertical layout, and consider a muted surface with a lighter border/shadow and 20–24px inner padding. That additional visual treatment was recommended, not applied without a request to implement it.
