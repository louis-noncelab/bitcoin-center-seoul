# 공개 디자인 체크포인트 커밋 — 2026-09-09

사용자 요청: “여기서 커밋을 한번 할까?” 현재 공개 디자인 결과와 직접 관련된 파일을 한 번의 로컬 체크포인트 커밋으로 기록한다. 푸시·병합·운영 배포 승인은 포함하지 않는다.

## 범위

- `web/`: Next.js 공개 프런트엔드, 한영 콘텐츠·디자인 시스템·모션, 직접 테스트, 설정/lockfile, 로컬 폰트와 SVG/라이선스.
- `docs/`: 원본 요청·부록, 디자인 결정, 구현/검증 기록과 최신 남은 작업 목록. 원본 캡처/동영상·실행 로그는 기존 ignore 정책을 유지한다.
- `.gitignore`, `AGENTS.md`: 승인된 작업 범위와 보존 규칙.
- `.github/workflows/deploy.yml` → `deploy.yml.disabled`: 이미 비활성화한 파일을 기록한다. rename similarity 92%의 차이는 공백 줄의 trailing whitespace 정리뿐이며 실행 내용은 동일하다. 이번 턴에 원본 내용을 변경하지 않았다.

기준 브랜치 `redesign/center-web`, 커밋 전 HEAD `fd7c472d856a257a77bf66766163b3f245a8757f`. 기존 upstream은 없다. 커밋 전 로컬 `origin/main` 추적 참조와 HEAD는 같았으며 원격 fetch나 push를 수행하지 않았다.

## 커밋 전 확인

- Node22 `npm run check`: 타입 검사와 ESLint 통과. 제품 코드·테스트를 이번 턴에 고치지 않았다.
- 직전 manifest의 소스/설정/자산/테스트 2568개와 실제 사진 7개를 확인했다. 처음 대조는 모두 일치했고, 이후 `web/README.md`의 최신 인수인계 포인터와 프로덕션 검사 명령만 갱신했다. 실행 코드/설정/테스트/자산에는 차이가 없다.
- 직전 build와 프로덕션 Playwright 141개, WebKit 등의 결과는 [기존 검증 기록](../handoff/2026-09-08-motion-footer-next.md)을 보존한다. 이번 커밋 턴에 브라우저/빌드를 다시 돌린 결과로 표현하지 않는다.
- staged 경로에 환경변수, 개인키, DB, 런타임 업로드, node_modules, 빌드/검사 결과, 세션 보관물, 증거 캡처/영상이 섞이지 않았음을 확인했다.
- 총괄은 공개 소스/설정과 staged diff를, 독립 담당 `commit_scope_check`는 문서/직접 테스트/라이선스/보존 범위와 자격증명 패턴을 읽기 전용으로 확인했다. 이 점검은 운영 보안 감사를 대체하지 않는다.
- 전체 `git diff --cached --check`는 45개 trailing-whitespace 표시로 exit 2: 기존 Markdown hard break 44개와 Pretendard OFL 원문 1개다. 원본 인수인계·문서 줄바꿈·라이선스를 보존한다. 해당 문서/원문을 제외한 제품·설정의 공백 검사는 통과했다. 전체 검사를 통과했다고 바꾸어 보고하지 않는다.
- `deploy.yml.disabled` SHA-256: `f8fd129d1b8d7deaba2198518fb7b8b84cf2b4ade001f184825d9f2f5844853d`. 활성 GitHub Actions 파일을 추가하지 않는다.

원본 검사 자료는 Git에서 제외된 `evidence/commit-2026-09-09/`에 있다. 이 문서를 포함한 체크포인트의 실제 SHA와 변경 목록은 `git log -1` 및 `git show --stat`으로 확인한다. 직전 보고서들의 옛 HEAD/fingerprint는 당시 검증 대상을 나타내므로 원문을 새 SHA로 덮어쓰지 않는다.

## 이어갈 곳

[남은 전체 작업](2026-09-09-remaining-work.md)과 [최신 공개 구현](../handoff/2026-09-08-motion-footer-next.md)을 기준으로 이어간다. 운영 자료 확보, 최종 사진/브랜드 마감, 관리자·DB, 이후 예약/결제, 운영 전환은 각각 남은 단계로 보존한다.
