# 서버(server.js) 전용 환경변수. 실제 값은 .env 에 두고, .env 는 git에 올리지 않는다.
# 서버 배포 시 /var/www/bitcoin-center-seoul/.env 에 이 파일을 복사해 값을 채운다.

# 관리자 비밀번호. /api/admin/login 과 x-admin-token 헤더 검증에 쓰인다. 클라이언트 번들에는 들어가지 않는다.
ADMIN_PASSWORD=change-me

# 선택: 기본값 3000 / 127.0.0.1
# PORT=3000
# HOST=127.0.0.1
